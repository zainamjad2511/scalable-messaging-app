# Redis

Redis is used as the centralized in-memory datastore and message broker (Pub/Sub) to synchronize state and broadcast messages across API replicas.

This folder is intended for:

- Local Redis configuration (if needed)
- Pub/Sub channel naming conventions
- Scripts for troubleshooting/testing Pub/Sub
- Notes on caching strategy and TTLs

