# Supabase / Postgres

Supabase provides the Postgres database used by the system.

This folder is intended for:

- SQL schema and migrations (when applicable)
- Notes on tables, indexes, and constraints (atomic operations for concurrency)
- Local development setup notes (if you use Supabase CLI)

