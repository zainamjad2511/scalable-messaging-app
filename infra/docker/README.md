# Docker

Docker-related helper assets.

This folder is intended for:

- Compose overrides (e.g. `docker-compose.override.yml`)
- Local dev Dockerfiles or shared build snippets
- Utility scripts/images to support running multiple API replicas

