# Apps

Deployable applications/services.

- `web/`: Next.js/React frontend (UI, auth UI, client-side caching, reconnect logic).
- `api/`: Node.js/Express API + WebSocket server. Designed to run multiple replicas behind Nginx.

