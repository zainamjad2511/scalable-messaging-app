# Packages

Shared code used by multiple apps.

- `types/`: Shared TypeScript types (DTOs, socket events, API response shapes).
- `shared/`: Shared runtime utilities/constants (e.g. validation helpers, env parsing, feature flags).

